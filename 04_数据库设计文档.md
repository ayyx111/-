# 校园版咸鱼 — 数据库设计文档

> 项目名称:校园版咸鱼(校园二手交易平台)
> 文档类型:数据库设计文档 (Database Design Document)
> 文档版本:V1.1
> 编写日期:2026-08-16
> 数据库:SQLite(主数据库) + Node-Cache(内存缓存)

---

## 一、设计概述

### 1.1 数据库选型

| 数据库 | 用途 | 说明 |
|--------|------|------|
| SQLite | 主数据库 | 文件型关系数据库,零配置部署,通过 better-sqlite3 驱动 + Sequelize ORM 操作 |
| Node-Cache | 缓存/会话 | 进程内内存缓存,替代 Redis,用于 Token 管理、热点数据缓存 |

### 1.2 命名规范

| 规范项 | 规则 | 示例 |
|--------|------|------|
| 表名 | 小写 + 下划线,复数 | `users`, `products` |
| 字段名 | 小写 + 下划线 | `user_id`, `created_at` |
| 主键 | `id`, BIGINT 自增 | `id` |
| 外键 | `表名单数_id` | `user_id`, `product_id` |
| 时间字段 | `_at` 后缀 | `created_at`, `updated_at` |
| 布尔字段 | `is_` 前缀 | `is_deleted`, `is_verified` |
| 字符集 | utf8mb4 | 支持表情符号 |
| 排序规则 | utf8mb4_unicode_ci | - |

---

## 二、ER 实体关系图

```
┌─────────┐       ┌──────────┐       ┌──────────┐
│  users  │       │ products │       │ categories│
│  用户   │◄─────►│  商品    │◄─────►│  分类    │
└────┬────┘       └────┬─────┘       └──────────┘
     │                 │
     │ 1:N             │ 1:N
     │                 │
     ▼                 ▼
┌─────────┐       ┌──────────┐
│ orders  │       │  images  │
│  订单   │       │  图片    │
└────┬────┘       └──────────┘
     │
     │ 1:1
     ▼
┌─────────┐
│ reviews │
│  评价   │
└─────────┘

┌─────────┐       ┌──────────┐
│  users  │◄─────►│ messages │
│  用户   │       │  消息    │
└────┬────┘       └──────────┘
     │
     │ 1:N
     ▼
┌──────────┐    ┌───────────┐    ┌──────────┐
│favorites │    │  reports  │    │notifications│
│ 收藏     │    │  举报     │    │  通知     │
└──────────┘    └───────────┘    └──────────┘
```

### 关系说明

| 关系 | 类型 | 说明 |
|------|------|------|
| 用户 ↔ 商品 | 1:N | 一个用户可发布多个商品 |
| 分类 ↔ 商品 | 1:N | 一个分类下有多个商品 |
| 商品 ↔ 图片 | 1:N | 一个商品有多张图片 |
| 用户 ↔ 订单 | 1:N | 买卖双方各关联订单 |
| 商品 ↔ 订单 | 1:1 | 一个商品对应一个订单 |
| 订单 ↔ 评价 | 1:1 | 一个订单对应一个评价 |
| 用户 ↔ 消息 | 1:N | 用户间发送消息 |
| 用户 ↔ 收藏 | 1:N | 用户收藏多个商品 |
| 用户 ↔ 举报 | 1:N | 用户举报商品/用户 |
| 用户 ↔ 通知 | 1:N | 用户接收多个通知 |

---

## 三、数据表设计

### 3.1 users(用户表)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 用户ID |
| username | VARCHAR(50) | NOT NULL, UNIQUE | 用户名 |
| password | VARCHAR(255) | NOT NULL | 密码(bcrypt加密) |
| email | VARCHAR(100) | UNIQUE | 邮箱 |
| phone | VARCHAR(20) | UNIQUE | 手机号 |
| student_id | VARCHAR(20) | UNIQUE | 学号 |
| avatar | VARCHAR(255) | DEFAULT NULL | 头像URL |
| nickname | VARCHAR(50) | DEFAULT NULL | 昵称 |
| bio | VARCHAR(200) | DEFAULT NULL | 个性签名 |
| school | VARCHAR(100) | DEFAULT NULL | 学校 |
| college | VARCHAR(100) | DEFAULT NULL | 学院 |
| enrollment_year | INT | DEFAULT NULL | 入学年份 |
| role | TINYINT | DEFAULT 0, NOT NULL | 角色:0用户 1管理员 |
| is_verified | TINYINT | DEFAULT 0, NOT NULL | 校园认证:0未认证 1已认证 2待审核 3未通过 |
| credit_score | INT | DEFAULT 100, NOT NULL | 信用分 |
| status | TINYINT | DEFAULT 1, NOT NULL | 状态:0封禁 1正常 |
| last_login_at | DATETIME | DEFAULT NULL | 最后登录时间 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**索引:**

| 索引名 | 字段 | 类型 |
|--------|------|------|
| uk_username | username | UNIQUE |
| uk_email | email | UNIQUE |
| uk_phone | phone | UNIQUE |
| uk_student_id | student_id | UNIQUE |
| idx_status | status | INDEX |

---

### 3.2 categories(分类表)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INT | PK, AUTO_INCREMENT | 分类ID |
| name | VARCHAR(50) | NOT NULL, UNIQUE | 分类名称 |
| icon | VARCHAR(255) | DEFAULT NULL | 分类图标 |
| sort_order | INT | DEFAULT 0 | 排序 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**初始数据:**

| id | name |
|----|------|
| 1 | 教材书籍 |
| 2 | 数码电子 |
| 3 | 生活用品 |
| 4 | 服装鞋包 |
| 5 | 运动器材 |
| 6 | 美妆护肤 |
| 7 | 其他 |

---

### 3.3 products(商品表)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 商品ID |
| user_id | BIGINT | NOT NULL, FK | 发布者ID |
| category_id | INT | NOT NULL, FK | 分类ID |
| title | VARCHAR(100) | NOT NULL | 商品标题 |
| description | TEXT | NOT NULL | 商品描述 |
| original_price | DECIMAL(10,2) | DEFAULT NULL | 原价 |
| price | DECIMAL(10,2) | NOT NULL | 售价 |
| condition_level | TINYINT | NOT NULL | 新旧:1全新 2几乎全新 3轻微使用 4有使用痕迹 |
| trade_type | TINYINT | NOT NULL | 交易方式:1面交 2邮寄 3均可 |
| location | VARCHAR(200) | DEFAULT NULL | 交易地点 |
| view_count | INT | DEFAULT 0 | 浏览量 |
| favorite_count | INT | DEFAULT 0 | 收藏数 |
| status | TINYINT | DEFAULT 0, NOT NULL | 状态:0待审核 1在售 2已锁定 3已售 4已下架 5审核拒绝 |
| ai_review_result | TINYINT | DEFAULT NULL | AI审核:0通过 1可疑 2违规 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**索引:**

| 索引名 | 字段 | 类型 |
|--------|------|------|
| idx_user_id | user_id | INDEX |
| idx_category_id | category_id | INDEX |
| idx_status | status | INDEX |
| idx_created_at | created_at | INDEX |
| idx_price | price | INDEX |
| idx_title_desc | title, description | FULLTEXT |

---

### 3.4 product_images(商品图片表)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 图片ID |
| product_id | BIGINT | NOT NULL, FK | 商品ID |
| image_url | VARCHAR(500) | NOT NULL | 图片URL |
| sort_order | INT | DEFAULT 0 | 排序 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**索引:**

| 索引名 | 字段 | 类型 |
|--------|------|------|
| idx_product_id | product_id | INDEX |

---

### 3.5 orders(订单表)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 订单ID |
| order_no | VARCHAR(32) | NOT NULL, UNIQUE | 订单编号 |
| product_id | BIGINT | NOT NULL, FK | 商品ID |
| buyer_id | BIGINT | NOT NULL, FK | 买家ID |
| seller_id | BIGINT | NOT NULL, FK | 卖家ID |
| price | DECIMAL(10,2) | NOT NULL | 成交价格 |
| trade_type | TINYINT | NOT NULL | 交易方式 |
| remark | VARCHAR(500) | DEFAULT NULL | 买家备注 |
| product_title | VARCHAR(100) | DEFAULT NULL | 商品标题(冗余,订单展示用) |
| status | TINYINT | DEFAULT 0, NOT NULL | 状态:0待确认 1待交易 2交易中 3已完成 4已取消 |
| buyer_confirmed | TINYINT | DEFAULT 0 | 买家确认:0否 1是 |
| seller_confirmed | TINYINT | DEFAULT 0 | 卖家确认:0否 1是 |
| confirmed_at | DATETIME | DEFAULT NULL | 交易完成时间 |
| canceled_at | DATETIME | DEFAULT NULL | 取消时间 |
| cancel_reason | VARCHAR(200) | DEFAULT NULL | 取消原因 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**索引:**

| 索引名 | 字段 | 类型 |
|--------|------|------|
| uk_order_no | order_no | UNIQUE |
| idx_product_id | product_id | INDEX |
| idx_buyer_id | buyer_id | INDEX |
| idx_seller_id | seller_id | INDEX |
| idx_status | status | INDEX |

---

### 3.6 reviews(评价表)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 评价ID |
| order_id | BIGINT | NOT NULL, UNIQUE, FK | 订单ID |
| from_user_id | BIGINT | NOT NULL, FK | 评价人ID |
| to_user_id | BIGINT | NOT NULL, FK | 被评价人ID |
| reviewer_role | TINYINT | NOT NULL | 评价人角色:1买家 2卖家 |
| rating | TINYINT | NOT NULL | 评分:1-5星 |
| content | VARCHAR(500) | DEFAULT NULL | 评价内容 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**索引:**

| 索引名 | 字段 | 类型 |
|--------|------|------|
| uk_order_id | order_id | UNIQUE |
| idx_from_user_id | from_user_id | INDEX |
| idx_to_user_id | to_user_id | INDEX |

---

### 3.7 messages(消息表)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 消息ID |
| from_user_id | BIGINT | NOT NULL, FK | 发送者ID |
| to_user_id | BIGINT | NOT NULL, FK | 接收者ID |
| product_id | BIGINT | DEFAULT NULL, FK | 关联商品ID |
| content | TEXT | NOT NULL | 消息内容 |
| msg_type | TINYINT | DEFAULT 0, NOT NULL | 类型:0文字 1图片 |
| is_read | TINYINT | DEFAULT 0, NOT NULL | 已读:0否 1是 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 发送时间 |

**索引:**

| 索引名 | 字段 | 类型 |
|--------|------|------|
| idx_from_to | from_user_id, to_user_id | INDEX |
| idx_product_id | product_id | INDEX |
| idx_to_user_read | to_user_id, is_read | INDEX |

---

### 3.8 favorites(收藏表)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 收藏ID |
| user_id | BIGINT | NOT NULL, FK | 用户ID |
| product_id | BIGINT | NOT NULL, FK | 商品ID |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 收藏时间 |

**索引:**

| 索引名 | 字段 | 类型 |
|--------|------|------|
| uk_user_product | user_id, product_id | UNIQUE |
| idx_user_id | user_id | INDEX |

---

### 3.9 reports(举报表)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 举报ID |
| reporter_id | BIGINT | NOT NULL, FK | 举报人ID |
| target_type | TINYINT | NOT NULL | 目标类型:1商品 2用户 |
| target_id | BIGINT | NOT NULL | 目标ID |
| reason | VARCHAR(200) | NOT NULL | 举报原因 |
| description | TEXT | DEFAULT NULL | 详细描述 |
| status | TINYINT | DEFAULT 0, NOT NULL | 状态:0待处理 1已处理 2已驳回 |
| handle_result | VARCHAR(200) | DEFAULT NULL | 处理结果 |
| handler_id | BIGINT | DEFAULT NULL, FK | 处理人(管理员)ID |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 举报时间 |
| handled_at | DATETIME | DEFAULT NULL | 处理时间 |

**索引:**

| 索引名 | 字段 | 类型 |
|--------|------|------|
| idx_reporter_id | reporter_id | INDEX |
| idx_target | target_type, target_id | INDEX |
| idx_status | status | INDEX |

---

### 3.10 notifications(通知表)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 通知ID |
| user_id | BIGINT | NOT NULL, FK | 接收用户ID |
| type | TINYINT | NOT NULL | 类型:1订单 2审核 3评价 4系统 5聊天 |
| title | VARCHAR(100) | NOT NULL | 通知标题 |
| content | VARCHAR(500) | NOT NULL | 通知内容 |
| related_id | BIGINT | DEFAULT NULL | 关联业务ID |
| is_read | TINYINT | DEFAULT 0, NOT NULL | 已读:0否 1是 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**索引:**

| 索引名 | 字段 | 类型 |
|--------|------|------|
| idx_user_read | user_id, is_read | INDEX |

---

### 3.11 user_browse_history(浏览记录表)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 记录ID |
| user_id | BIGINT | NOT NULL, FK | 用户ID |
| product_id | BIGINT | NOT NULL, FK | 商品ID |
| category_id | INT | DEFAULT NULL | 商品分类(冗余,加速推荐) |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 浏览时间 |

**索引:**

| 索引名 | 字段 | 类型 |
|--------|------|------|
| idx_user_id | user_id | INDEX |
| idx_user_category | user_id, category_id | INDEX |

---

### 3.12 admin_logs(管理员操作日志表)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 日志ID |
| admin_id | BIGINT | NOT NULL, FK | 管理员ID |
| action | VARCHAR(50) | NOT NULL | 操作类型 |
| target_type | VARCHAR(50) | DEFAULT NULL | 目标类型 |
| target_id | BIGINT | DEFAULT NULL | 目标ID |
| detail | TEXT | DEFAULT NULL | 操作详情 |
| ip_address | VARCHAR(45) | DEFAULT NULL | 操作IP |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 操作时间 |

---

## 四、Node-Cache 缓存设计

### 4.1 缓存设计

| 缓存键 | 数据类型 | 说明 | TTL |
|--------|----------|------|-----|
| `token:refresh:{userId}` | String | Refresh Token | 7天 |
| `token:blacklist:{jti}` | String | Token黑名单 | 2小时 |
| `user:info:{userId}` | Object | 用户信息缓存 | 1小时 |
| `product:hot` | Array | 热门商品列表 | 1小时 |
| `product:detail:{productId}` | Object | 商品详情缓存 | 30分钟 |
| `category:list` | Array | 分类列表缓存 | 24小时 |
| `search:hot` | Array | 热搜词列表 | 1小时 |
| `rate:login:{ip}` | Number | 登录限流计数 | 1分钟 |
| `view:product:{productId}:{userId}` | Boolean | 防重复浏览标记 | 10分钟 |

### 4.2 会话管理

```javascript
// Refresh Token 存储
cache.set('token:refresh:123', 'eyJhbGciOiJIUzI1NiIs...', 604800);

// 登录限流(每分钟5次)
const count = cache.get('rate:login:192.168.1.1') || 0;
cache.set('rate:login:192.168.1.1', count + 1, 60);

// 热门商品(基于浏览量)
const hotProducts = cache.get('product:hot') || [];
```

---

## 五、建表 SQL 脚本

> 注:项目实际使用 Sequelize ORM 自动建表(sequelize.sync),以下 SQL 供参考。
> 完整SQL脚本见: `database/init.sql`

```sql
-- 创建数据库(SQLite 自动创建,无需手动建库)

-- 用户表
CREATE TABLE users (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  username VARCHAR(50) NOT NULL UNIQUE COMMENT '用户名',
  password VARCHAR(255) NOT NULL COMMENT '密码(bcrypt)',
  email VARCHAR(100) UNIQUE COMMENT '邮箱',
  phone VARCHAR(20) UNIQUE COMMENT '手机号',
  student_id VARCHAR(20) UNIQUE COMMENT '学号',
  avatar VARCHAR(255) DEFAULT NULL COMMENT '头像URL',
  nickname VARCHAR(50) DEFAULT NULL COMMENT '昵称',
  bio VARCHAR(200) DEFAULT NULL COMMENT '个性签名',
  school VARCHAR(100) DEFAULT NULL COMMENT '学校',
  college VARCHAR(100) DEFAULT NULL COMMENT '学院',
  enrollment_year INT DEFAULT NULL COMMENT '入学年份',
  role TINYINT NOT NULL DEFAULT 0 COMMENT '角色:0用户 1管理员',
  is_verified TINYINT NOT NULL DEFAULT 0 COMMENT '认证:0未认证 1已认证 2待审核 3未通过',
  credit_score INT NOT NULL DEFAULT 100 COMMENT '信用分',
  status TINYINT NOT NULL DEFAULT 1 COMMENT '状态:0封禁 1正常',
  last_login_at DATETIME DEFAULT NULL COMMENT '最后登录时间',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户表';

-- 分类表
CREATE TABLE categories (
  id INT AUTO_INCREMENT PRIMARY KEY,
  name VARCHAR(50) NOT NULL UNIQUE COMMENT '分类名称',
  icon VARCHAR(255) DEFAULT NULL COMMENT '分类图标',
  sort_order INT DEFAULT 0 COMMENT '排序',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='分类表';

-- 商品表
CREATE TABLE products (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  user_id BIGINT NOT NULL COMMENT '发布者ID',
  category_id INT NOT NULL COMMENT '分类ID',
  title VARCHAR(100) NOT NULL COMMENT '商品标题',
  description TEXT NOT NULL COMMENT '商品描述',
  original_price DECIMAL(10,2) DEFAULT NULL COMMENT '原价',
  price DECIMAL(10,2) NOT NULL COMMENT '售价',
  condition_level TINYINT NOT NULL COMMENT '新旧:1全新 2几乎全新 3轻微使用 4有使用痕迹',
  trade_type TINYINT NOT NULL COMMENT '交易方式:1面交 2邮寄 3均可',
  location VARCHAR(200) DEFAULT NULL COMMENT '交易地点',
  view_count INT DEFAULT 0 COMMENT '浏览量',
  favorite_count INT DEFAULT 0 COMMENT '收藏数',
  status TINYINT NOT NULL DEFAULT 0 COMMENT '状态:0待审核 1在售 2已锁定 3已售 4已下架 5审核拒绝',
  ai_review_result TINYINT DEFAULT NULL COMMENT 'AI审核:0通过 1可疑 2违规',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  INDEX idx_user_id (user_id),
  INDEX idx_category_id (category_id),
  INDEX idx_status (status),
  INDEX idx_created_at (created_at),
  INDEX idx_price (price),
  FULLTEXT INDEX idx_title_desc (title, description),
  CONSTRAINT fk_product_user FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  CONSTRAINT fk_product_category FOREIGN KEY (category_id) REFERENCES categories(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='商品表';

-- 商品图片表
CREATE TABLE product_images (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  product_id BIGINT NOT NULL COMMENT '商品ID',
  image_url VARCHAR(500) NOT NULL COMMENT '图片URL',
  sort_order INT DEFAULT 0 COMMENT '排序',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  INDEX idx_product_id (product_id),
  CONSTRAINT fk_image_product FOREIGN KEY (product_id) REFERENCES products(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='商品图片表';

-- 订单表
CREATE TABLE orders (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  order_no VARCHAR(32) NOT NULL UNIQUE COMMENT '订单编号',
  product_id BIGINT NOT NULL COMMENT '商品ID',
  buyer_id BIGINT NOT NULL COMMENT '买家ID',
  seller_id BIGINT NOT NULL COMMENT '卖家ID',
  price DECIMAL(10,2) NOT NULL COMMENT '成交价格',
  trade_type TINYINT NOT NULL COMMENT '交易方式',
  remark VARCHAR(500) DEFAULT NULL COMMENT '买家备注',
  status TINYINT NOT NULL DEFAULT 0 COMMENT '状态:0待确认 1待交易 2交易中 3已完成 4已取消',
  buyer_confirmed TINYINT DEFAULT 0 COMMENT '买家确认',
  seller_confirmed TINYINT DEFAULT 0 COMMENT '卖家确认',
  confirmed_at DATETIME DEFAULT NULL COMMENT '交易完成时间',
  canceled_at DATETIME DEFAULT NULL COMMENT '取消时间',
  cancel_reason VARCHAR(200) DEFAULT NULL COMMENT '取消原因',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  INDEX idx_product_id (product_id),
  INDEX idx_buyer_id (buyer_id),
  INDEX idx_seller_id (seller_id),
  INDEX idx_status (status),
  CONSTRAINT fk_order_product FOREIGN KEY (product_id) REFERENCES products(id),
  CONSTRAINT fk_order_buyer FOREIGN KEY (buyer_id) REFERENCES users(id),
  CONSTRAINT fk_order_seller FOREIGN KEY (seller_id) REFERENCES users(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='订单表';

-- 评价表
CREATE TABLE reviews (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  order_id BIGINT NOT NULL UNIQUE COMMENT '订单ID',
  from_user_id BIGINT NOT NULL COMMENT '评价人ID',
  to_user_id BIGINT NOT NULL COMMENT '被评价人ID',
  rating TINYINT NOT NULL COMMENT '评分1-5',
  content VARCHAR(500) DEFAULT NULL COMMENT '评价内容',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  INDEX idx_from_user_id (from_user_id),
  INDEX idx_to_user_id (to_user_id),
  CONSTRAINT fk_review_order FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE CASCADE,
  CONSTRAINT fk_review_from FOREIGN KEY (from_user_id) REFERENCES users(id),
  CONSTRAINT fk_review_to FOREIGN KEY (to_user_id) REFERENCES users(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='评价表';

-- 消息表
CREATE TABLE messages (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  from_user_id BIGINT NOT NULL COMMENT '发送者ID',
  to_user_id BIGINT NOT NULL COMMENT '接收者ID',
  product_id BIGINT DEFAULT NULL COMMENT '关联商品ID',
  content TEXT NOT NULL COMMENT '消息内容',
  msg_type TINYINT NOT NULL DEFAULT 0 COMMENT '类型:0文字 1图片',
  is_read TINYINT NOT NULL DEFAULT 0 COMMENT '已读:0否 1是',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '发送时间',
  INDEX idx_from_to (from_user_id, to_user_id),
  INDEX idx_product_id (product_id),
  INDEX idx_to_user_read (to_user_id, is_read),
  CONSTRAINT fk_msg_from FOREIGN KEY (from_user_id) REFERENCES users(id),
  CONSTRAINT fk_msg_to FOREIGN KEY (to_user_id) REFERENCES users(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='消息表';

-- 收藏表
CREATE TABLE favorites (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  user_id BIGINT NOT NULL COMMENT '用户ID',
  product_id BIGINT NOT NULL COMMENT '商品ID',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '收藏时间',
  UNIQUE KEY uk_user_product (user_id, product_id),
  INDEX idx_user_id (user_id),
  CONSTRAINT fk_fav_user FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  CONSTRAINT fk_fav_product FOREIGN KEY (product_id) REFERENCES products(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='收藏表';

-- 举报表
CREATE TABLE reports (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  reporter_id BIGINT NOT NULL COMMENT '举报人ID',
  target_type TINYINT NOT NULL COMMENT '目标类型:1商品 2用户',
  target_id BIGINT NOT NULL COMMENT '目标ID',
  reason VARCHAR(200) NOT NULL COMMENT '举报原因',
  description TEXT DEFAULT NULL COMMENT '详细描述',
  status TINYINT NOT NULL DEFAULT 0 COMMENT '状态:0待处理 1已处理 2已驳回',
  handle_result VARCHAR(200) DEFAULT NULL COMMENT '处理结果',
  handler_id BIGINT DEFAULT NULL COMMENT '处理人ID',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '举报时间',
  handled_at DATETIME DEFAULT NULL COMMENT '处理时间',
  INDEX idx_reporter_id (reporter_id),
  INDEX idx_target (target_type, target_id),
  INDEX idx_status (status),
  CONSTRAINT fk_report_user FOREIGN KEY (reporter_id) REFERENCES users(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='举报表';

-- 通知表
CREATE TABLE notifications (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  user_id BIGINT NOT NULL COMMENT '接收用户ID',
  type TINYINT NOT NULL COMMENT '类型:1订单 2审核 3评价 4系统 5聊天',
  title VARCHAR(100) NOT NULL COMMENT '通知标题',
  content VARCHAR(500) NOT NULL COMMENT '通知内容',
  related_id BIGINT DEFAULT NULL COMMENT '关联业务ID',
  is_read TINYINT NOT NULL DEFAULT 0 COMMENT '已读:0否 1是',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  INDEX idx_user_read (user_id, is_read),
  CONSTRAINT fk_notif_user FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='通知表';

-- 浏览记录表
CREATE TABLE user_browse_history (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  user_id BIGINT NOT NULL COMMENT '用户ID',
  product_id BIGINT NOT NULL COMMENT '商品ID',
  category_id INT DEFAULT NULL COMMENT '商品分类(冗余)',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '浏览时间',
  INDEX idx_user_id (user_id),
  INDEX idx_user_category (user_id, category_id),
  CONSTRAINT fk_history_user FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  CONSTRAINT fk_history_product FOREIGN KEY (product_id) REFERENCES products(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='浏览记录表';

-- 管理员操作日志表
CREATE TABLE admin_logs (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  admin_id BIGINT NOT NULL COMMENT '管理员ID',
  action VARCHAR(50) NOT NULL COMMENT '操作类型',
  target_type VARCHAR(50) DEFAULT NULL COMMENT '目标类型',
  target_id BIGINT DEFAULT NULL COMMENT '目标ID',
  detail TEXT DEFAULT NULL COMMENT '操作详情',
  ip_address VARCHAR(45) DEFAULT NULL COMMENT '操作IP',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '操作时间',
  CONSTRAINT fk_log_admin FOREIGN KEY (admin_id) REFERENCES users(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='管理员操作日志表';

-- 初始化分类数据
INSERT INTO categories (name, sort_order) VALUES
('教材书籍', 1), ('数码电子', 2), ('生活用品', 3),
('服装鞋包', 4), ('运动器材', 5), ('美妆护肤', 6), ('其他', 7);

-- 初始化管理员账号 (密码: admin123, bcrypt加密)
INSERT INTO users (username, password, role, is_verified, status, nickname) VALUES
('admin', '$2b$10$N9qo8uLOickgx2ZMRZoMyeIjZAgcfl7p92ldGxad68LJZdL17lhWy', 1, 1, 1, '系统管理员');
```

---

## 六、数据备份策略

| 备份类型 | 频率 | 保留 | 方式 |
|----------|------|------|------|
| 数据库文件备份 | 每日凌晨3点 | 7天 | 复制 database.sqlite 文件 |
| 缓存数据 | 重启自动重建 | - | Node-Cache 进程内缓存 |

> 注:SQLite 为单文件数据库,备份时仅需复制 `database.sqlite` 文件即可。
> 生产环境如需更高并发,可平滑迁移至 MySQL,Sequelize ORM 支持无缝切换。

---

> 本数据库设计文档经评审后作为开发依据。
